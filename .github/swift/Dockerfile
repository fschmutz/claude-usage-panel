# The Swift toolchain the swift-core CI job builds, tests and format-lints the
# Foundation-only core with. Pinned by digest: a tag is mutable. It lives in a
# Dockerfile, not in the workflow, so Dependabot's docker ecosystem can see it
# and bump the digest.
FROM swift:6.4-noble@sha256:64bab762bc73a3fda6d9ebc559258bd6d7660c10a705bb25ecacad2f99d066f9
